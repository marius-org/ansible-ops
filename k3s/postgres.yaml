apiVersion: v1
kind: Namespace
metadata:
  name: ansible-ops
---
# Create secret manually — never commit to git:
# kubectl create secret generic ansible-secret -n ansible-ops \
#   --from-literal=POSTGRES_DB=ansibledb \
#   --from-literal=POSTGRES_USER=ansible \
#   --from-literal=POSTGRES_PASSWORD=ansiblepass \
#   --from-literal=DATABASE_URL="postgresql://ansible:ansiblepass@postgres-service:5432/ansibledb"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: ansible-ops
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: ansible-ops
spec:
  serviceName: postgres-service
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: ansible-secret
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: ansible-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ansible-secret
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-pvc
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
  volumeClaimTemplates:
    - metadata:
        name: postgres-pvc
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: nfs
        resources:
          requests:
            storage: 2Gi
